# 控制



1.matlab中nmpc工具箱



## **目标函数**

### 1.自定义目标函数的格式：

```
function J = CustomCostFcn(X, U, e, data)
    % X: 状态变量矩阵 (PredictionHorizon+1 x nx)
    % U: 控制输入矩阵 (PredictionHorizon x nu)
    % e: 软约束松弛变量 (PredictionHorizon x ny)
    % data: 附加信息结构
    % 返回值 J: 目标函数标量值
end
```

#### **输入参数详解**

1. **`X`**: 状态变量矩阵

   - 行数为 `PredictionHorizon+1`，表示预测范围内的所有状态，+1是因为包括了初始状态。

   - 列数为状态变量的维度 `nx`。

   - 格式为：

     ```
     X = [x1(t), x2(t), ..., xn(t);     x1(t+1), x2(t+1), ..., xn(t+1);     ...     x1(t+PredictionHorizon), ..., xn(t+PredictionHorizon)]
     ```

2. **`U`**: 控制输入矩阵

   - 行数为 `PredictionHorizon`，表示预测范围内的所有控制输入。

   - 列数为控制输入变量的数量 `nu`。

   - 格式为：

     ```
     U = [u1(t), u2(t), ..., um(t);     u1(t+1), u2(t+1), ..., um(t+1);     ...     u1(t+PredictionHorizon-1), ..., um(t+PredictionHorizon-1)]
     ```

3. **`e`**: 松弛变量（Soft Constraints）

   - 如果使用了软约束，则该变量表示松弛程度。
   - 如果没有定义软约束，则 `e` 会为空矩阵。

4. **`data`**: 附加信息结构

   - 包含一些模型、控制器的附加数据，例如时间步长、当前时间等。
   - 具体内容可以根据需要通过 `disp(data)` 查看。

### **返回值**

- **`J`**: 返回标量目标值，是 NMPC 优化过程中需要最小化的量。



将额外的参数传递给目标函数：

### **您可以通过匿名函数将额外参数传递给 `CustomCostFcn`

```matlab
nlobj = nlmpc(2, 2, 1);
% 定义额外的参数
extraParams = struct('Q', diag([1, 1]), 'R', 0.1);

% 使用匿名函数传递参数
nlobj.Optimization.CustomCostFcn = @(X, U, e, data) CustomCostWithExtraParams(X, U, e, data, extraParams); %匿名函数传递额外参数

% 自定义目标函数
function J = CustomCostWithExtraParams(X, U, e, data, params)
    % 从 params 中提取权重
    Q = params.Q;
    R = params.R;

    % 计算状态偏差代价
    stateCost = sum(sum(X * Q .* X));
    controlCost = sum(sum(U * R .* U));
    J = stateCost + controlCost;
end

```





## 约束条件

`Optimization.CustomEqConFcn` 定义的约束函数应遵循以下格式：

```matlab
function Ceq = CustomEqConFcn(X, U, data)
    % X: 状态变量矩阵 (PredictionHorizon+1 x nx)
    % U: 控制输入矩阵 (PredictionHorizon x nu)
    % data: 附加信息结构
    % Ceq: 等式约束的列向量

    t = (0:size(X, 1)-1)' * data.Ts;
    target1 = sin(t);   % 目标轨迹 1
    target2 = cos(t);   % 目标轨迹 2
    % 约束 x1 和 x2 分别跟随目标轨迹
    constraint1 = X(:, 1) - target1;
    constraint2 = X(:, 2) - target2;
    % 合并约束
    Ceq = [constraint1; constraint2];
end
```

### **输入参数说明**

1. **`X`**: 状态变量矩阵
   - 尺寸：`(PredictionHorizon+1) x nx`。
   - 每一行代表预测范围内的一个时间点的状态变量。
2. **`U`**: 控制输入矩阵
   - 尺寸：`PredictionHorizon x nu`。
   - 每一行代表预测范围内的一个时间点的控制输入。
3. **`data`**: 附加信息结构
   - 包含控制器、模型的附加信息，常见内容包括预测范围 (`PredictionHorizon`)、控制范围 (`ControlHorizon`)、时间步长 (`Ts`)、用户自定义参数等。

**输出参数说明**

**`Ceq`**: 返回的一系列等式，维度为 `(numConstraints x 1)`，有多少个约束条件，尺寸就是多大。相当于自动让Ceq为0



我有一个问题关于mpc做目标制导，我的思路是获取每个时刻的目标位置，然后让我方无人机做mpc往这个目标点进行制导，实现和目标i精确撞击。但是这样会有一个问题，就是任意一个时刻无人机追踪的目标点都是上一时刻目标的位置，意思就是在前往目标的过程中，目标已经移动了，如何解决永远迟一步的问题

你提到的问题是经典的**追踪延迟问题**，这是在多步预测控制（MPC）制导中常见的挑战，尤其是在目标具有动态行为时。

为了解决“永远迟一步”的问题，可以采用以下思路优化你的MPC制导方案：

------

### 1. **引入目标的运动预测模型**

目标的位置变化可以通过预测来弥补延迟问题。具体做法是：

- **目标运动建模**：使用目标的历史运动数据（位置和速度），通过简单的物理模型（如匀速直线运动、匀加速运动）或者复杂的学习模型（如卡尔曼滤波、深度学习模型）来预测目标的未来位置。
- **预测未来位置**：在每个时刻，用预测模型估计目标的下一时刻位置 xtarget(t+T)\mathbf{x}_\text{target}(t + T)xtarget(t+T)，将其作为当前MPC的参考目标点。

> **示例**：
> 如果目标以匀速运动 vtargetv_\text{target}vtarget​，预测的目标位置可以简单计算为： xtarget(t+T)=xtarget(t)+vtarget⋅T\mathbf{x}_\text{target}(t + T) = \mathbf{x}_\text{target}(t) + \mathbf{v}_\text{target} \cdot Txtarget​(t+T)=xtarget​(t)+vtarget​⋅T
> 其中 TTT 是预测的时间步长。

------

### 2. **减少控制系统延迟**

系统延迟包括感知延迟、计算延迟和执行延迟。为了尽量减少这些延迟，可以：

- **优化MPC计算**：降低MPC优化问题的复杂度，减少求解时间（如减少预测时域长度、简化约束条件等）。
- **快速采样更新**：增大控制器的刷新频率，使无人机可以更频繁地调整轨迹。
- **多线程并行计算**：感知与MPC优化计算分离，提高实时性。

------

### 3. **引入前馈控制**

将目标的加速度或速度信息引入到控制模型中。通过前馈控制，补偿目标运动导致的滞后现象。可以在MPC的成本函数中加入目标的动态项，例如： J=∑k=0N∥xdrone(tk)−xtarget(tk)∥2+λ∥vdrone(tk)−vtarget(tk)∥2

这里vtarget(tk) 是目标的预测速度，帮助引导无人机提前规划加速方向。

------

### 4. **非线性模型预测控制（NMPC）**

针对动态目标，NMPC可以更有效地处理非线性行为。在NMPC中，可以将目标运动模型直接嵌入预测过程中：

- 定义目标轨迹预测模型。
- 在优化时考虑目标未来的位置变化，从而生成更“前瞻性”的轨迹。

------

### 5. **动态调整预测时域长度**

当目标移动速度较快时，并且和目标距离较近时，可以动态缩短MPC的预测时域长度，使无人机快速响应当前目标位置的变化；

当目标移动速度较慢时，并且和目标距离较远时，可以加长预测时域以规划更平滑的轨迹。

------

### 6. **强化学习辅助MPC**

结合MPC和强化学习，使用强化学习（RL）模型预测目标动态行为，或直接学习最佳的制导策略：

- RL模型可以根据历史数据学习目标的复杂运动模式。
- MPC则专注于短时内无人机的精确制导，二者结合实现全局和局部的有效优化。

------

### 总结

关键是引入目标的**未来位置预测**，并结合实时性优化控制器。推荐先从简单的目标运动模型（如匀速预测）开始实验，然后逐步加入复杂的预测和优化策略。





