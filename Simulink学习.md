# Simulink学习



仿真步长：按住ctrl+E，可以看到默认仿真步长时0.01秒

## clock

- **抽取值**：

  - 抽取值是一个正整数，表示每隔多少个仿真步长输出一次时间值。
  - 例如，如果抽取值为 `10`，则 **Clock** 模块会每隔 10 个仿真步长输出一次时间值，也就是每隔0.1秒，从仿真中提取一个时间数据。

- **默认值**：

  - 默认抽取值为 `1`，表示 **Clock** 模块在每个仿真步长都输出时间值。

  

## Sin Wave

| **特性**       | **基于时间（Time-based）**                 | **基于采样（Sample-based）**                       |
| :------------- | :----------------------------------------- | :------------------------------------------------- |
| **时间源**     | 仿真时间或外部信号                         | 采样点索引                                         |
| **连续性**     | 连续信号                                   | 离散信号                                           |
| **适用场景**   | 连续系统仿真                               | 离散系统仿真                                       |
| **公式**       | y=A⋅sin⁡(2πft+ϕ)*y*=*A*⋅sin(2*π**f**t*+*ϕ*) | y=A⋅sin⁡(2πfnTs+ϕ)*y*=*A*⋅sin(2*π**f**n**T**s*+*ϕ*) |
| **时间源选项** | 仿真时间或外部信号                         | 无                                                 |

**基于时间的方式：**

可与选择外部时间，外部时间由**clock**提供，只需要在sin wave模块前面连接一个clock就可以，按照指定的频率生成数据

可以采用仿真时间，在最底下的参数里定义采样时间

| **特性**       | **外部时间（Use external signal）**      | **仿真时间（Use simulation time）**                    |
| :------------- | :--------------------------------------- | :----------------------------------------------------- |
| **时间源**     | 由外部信号提供，所有模块共享同一时间源。 | 每个模块独立使用Simulink的仿真时间。                   |
| **同步性**     | 高，所有模块的时间基准一致。             | 低，模块的时间基准可能不一致（尤其是采样时间不同时）。 |
| **频率一致性** | 高，所有模块的频率由外部信号决定。       | 低，模块的频率可能不同（尤其是采样时间不同时）。       |
| **灵活性**     | 固定时间源，无法动态调整。               | 高度灵活，时间源可以动态变化。                         |
| **典型应用**   | 标准正弦信号生成、连续系统仿真。         | 非线性时间变化、动态频率调整、自定义时间控制。         |



